<Window x:Class="Project.Views.User.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project.Views.User"
        xmlns:vm="clr-namespace:Project.ViewModels.UserVm"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.DataContext>
        <vm:MainWindowVM/>
    </Window.DataContext>-->   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <UniformGrid Grid.Row="0">
            <Button Content="Home" Command="{Binding openHome}"/>
            <Button Content="Profile" Command="{Binding openProfile}"/>

        </UniformGrid>
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:HomeVM }">
                    <local:HomeView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:ProfileVm }">
                    <local:ProfileView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
